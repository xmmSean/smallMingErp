<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smallming.erp.mapper.order.OrderItemMapper">

    <insert id="insert" parameterType="com.smallming.erp.domain.order.OrderItem">
        insert into order_item (
            tenant_id,
            id,
            order_id,
            inventory_id,
            sku_id,
            quantity,
            unit_price,
            total_amount,
            created_at
        ) values (
            #{tenantId},
            #{id},
            #{orderId},
            #{inventoryId},
            #{skuId},
            #{quantity},
            #{unitPrice},
            #{totalAmount},
            #{createdAt}
        )
    </insert>

</mapper>